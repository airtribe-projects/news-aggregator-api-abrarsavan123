# News Aggregator API

A RESTful API built with Node.js and Express that lets users register, log in, manage their news preferences, and fetch personalised news articles.

---

## Prerequisites

- Node.js >= 18.0.0 (uses the built-in `fetch` API)
- npm >= 8.x

---

## Setup

```sh
cd "News Aggregator API"
npm install
